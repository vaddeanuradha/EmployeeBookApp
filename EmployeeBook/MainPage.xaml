<Page
    x:Class="EmployeeBookApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EmployeeBook"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Orientation="Horizontal" Margin="50,0,50,0">
            <TextBlock Text="Name: " Margin="10" Width="100"/>
            <TextBlock Text="{Binding Name}" Margin="10" Width="100"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="50,50,0,0">
            <TextBlock Text="Title: " Margin="10" Width="100"/>
            <TextBlock Text="{Binding Title}" Margin="10" Width="100"/>

            <Button Content="Add Employee" Click="Button_Click" HorizontalAlignment="Center" VerticalAlignment="Center" Height="80" Width="526" RenderTransformOrigin="0.943,1.138" Margin="0,0,0,0" />
        </StackPanel>


    </Grid>
</Page>
